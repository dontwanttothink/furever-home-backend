{
  "name": "app",
  "version": "1.0.50",
  "module": "src/index.js",
  "devDependencies": {
    "@biomejs/biome": "1.9.4",
    "bun-types": "latest"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "bun run --watch src/main.ts"
  },
  "dependencies": {
    "path-to-regexp": "^8.2.0"
  }
}
