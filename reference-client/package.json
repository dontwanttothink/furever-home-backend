{
	"name": "furever-home-reference-client",
	"module": "index.ts",
	"type": "module",
	"scripts": {
		"build": "bunx tsc --noEmit && bunx bunx esbuild ts/index.ts --tsconfig=tsconfig.json --format=esm --outdir=public/js --platform=browser --out-extension:.js=.mjs --bundle --target=esnext --sourcemap"
	},
	"devDependencies": {
		"esbuild": "^0.25.0"
	},
	"peerDependencies": {
		"typescript": "^5.0.0"
	}
}
